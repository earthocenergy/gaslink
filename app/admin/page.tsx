"use client";import {useEffect,useState} from "react";import Link from "next/link";import BackButton from "@/components/BackButton";import {createClient} from "@/lib/supabase/client";import {ShieldCheck,Store,Wrench,Building2,RadioTower,MessageSquare,ClipboardCheck,ArrowRight} from "lucide-react";
export default function Admin(){const [ok,setOk]=useState<boolean|null>(null),[data,setData]=useState<any>({claims:[],reports:[],sellers:[],listings:[],providers:[],offerings:[],business:[],registrations:[]});async function load(){const db=createClient(),{data:{user}}=await db.auth.getUser();if(!user){location.href="/auth";return}const {data:p}=await db.from("profiles").select("role").eq("id",user.id).single();if(p?.role!=="admin"){setOk(false);return}setOk(true);const [claims,reports,sellers,listings,providers,offerings,business,registrations]=await Promise.all([db.from("station_claims").select("id,status,business_name,phone,stations(name)").order("created_at",{ascending:false}),db.from("station_reports").select("id,status,is_moderated,stations(name)").order("created_at",{ascending:false}),db.from("marketplace_sellers").select("*").order("created_at",{ascending:false}),db.from("marketplace_listings").select("id,title,status,category,seller_id,equipment_verification_status").order("created_at",{ascending:false}),db.from("service_providers").select("*").order("created_at",{ascending:false}),db.from("service_offerings").select("id,title,status,category,provider_id").order("created_at",{ascending:false}),db.from("business_enquiries").select("*").order("created_at",{ascending:false}),db.from("stations").select("id,name,operator_name,address,city,state,registration_status,submitted_by").eq("registration_status","pending").order("created_at",{ascending:false})]);setData({claims:claims.data||[],reports:reports.data||[],sellers:sellers.data||[],listings:listings.data||[],providers:providers.data||[],offerings:offerings.data||[],business:business.data||[],registrations:registrations.data||[]})}useEffect(()=>{load()},[]);
async function rpc(fn:string,id:string,status:string){const names:any={admin_marketplace_seller:"p_id",admin_marketplace_listing:"p_id",admin_service_provider:"p_id",admin_service_offering:"p_id"};const {error}=await createClient().rpc(fn,{[names[fn]]:id,p_status:status});if(error)alert(error.message);else load()}async function claim(id:string,a:boolean){const {error}=await createClient().rpc("approve_station_claim",{p_claim_id:id,p_approve:a});if(error)alert(error.message);else load()}async function moderate(id:string,apply:boolean){const {error}=await createClient().rpc("admin_moderate_station_report",{p_report_id:id,p_apply:apply});if(error)alert(error.message);else load()}async function businessStatus(id:string,status:string){const {error}=await createClient().rpc("admin_update_business_enquiry",{p_id:id,p_status:status});if(error)alert(error.message);else load()}async function equipmentStatus(id:string,status:string){const {error}=await createClient().rpc("admin_equipment_verification",{p_id:id,p_status:status});if(error)alert(error.message);else load()}async function registration(id:string,approve:boolean){const {error}=await createClient().rpc("admin_review_station_registration",{p_station_id:id,p_approve:approve});if(error)alert(error.message);else load()}
const buttons=(fn:string,id:string,status:string,verified="approved")=>status==="pending"?<p><button className="primary" onClick={()=>rpc(fn,id,verified)}>Approve</button> <button className="secondary" onClick={()=>rpc(fn,id,"rejected")}>Reject</button></p>:null;
if(ok===null)return <main className="panel">Checking Earthoc Admin access…</main>;if(!ok)return <main className="panel"><h1>Admin access required</h1><Link href="/" className="primary">Return to GasLink</Link></main>;
return <main><header><Link href="/" className="brand"><span className="mark">G</span><div><b>GasLink</b><small>Earthoc Admin</small></div></Link><nav className="topNav"><Link href="/stations">Network</Link><Link href="/marketplace">Marketplace</Link><Link href="/services">Services</Link><Link href="/business">Business</Link></nav><BackButton fallback="/dashboard"/></header><section className="adminHero"><div><span className="eyebrow"><ShieldCheck size={14}/> EARTHOC OPERATIONS</span><h1>Run the GasLink ecosystem.</h1><p>Review network submissions, marketplace activity, service providers, community intelligence and commercial demand from one control centre.</p></div><Link href="/operator" className="secondary">Operator view <ArrowRight size={15}/></Link></section><section className="adminWrap adminV2"><div className="adminStats adminStatsV2"><div><RadioTower/><b>{data.registrations.length}</b><span>Station registrations</span></div><div><Store/><b>{data.listings.filter((x:any)=>x.status==="pending").length}</b><span>Listings pending</span></div><div><Wrench/><b>{data.providers.filter((x:any)=>x.verification_status==="pending").length}</b><span>Providers pending</span></div><div><Building2/><b>{data.business.length}</b><span>Business leads</span></div><div><MessageSquare/><b>{data.reports.filter((x:any)=>!x.is_moderated).length}</b><span>Reports awaiting review</span></div></div><div className="adminSectionIntro"><ClipboardCheck/><div><span className="eyebrow">Moderation queues</span><h2>Review & operations</h2><p>Work through pending items below. Approval actions remain connected to the existing protected admin workflows.</p></div></div>
<h2 className="adminSectionTitle">Seller verification<span>Marketplace sellers</span></h2><div className="adminList">{data.sellers.length?data.sellers.map((x:any)=><div key={x.id}><b>{x.business_name}</b><span>{x.city||""} {x.state||""} · {x.verification_status}</span>{buttons("admin_marketplace_seller",x.id,x.verification_status,"verified")}</div>):<p className="muted">No sellers yet.</p>}</div>
<h2 className="adminSectionTitle">Equipment marketplace<span>Marketplace listings</span></h2><div className="adminList">{data.listings.length?data.listings.map((x:any)=><div key={x.id}><b>{x.title}</b><span>{x.category} · {x.status}</span>{buttons("admin_marketplace_listing",x.id,x.status)}<p>Equipment: <b>{x.equipment_verification_status}</b> <select value={x.equipment_verification_status} onChange={e=>equipmentStatus(x.id,e.target.value)}><option value="unverified">Unverified</option><option value="reviewing">Reviewing</option><option value="verified">Verified</option><option value="rejected">Rejected</option></select></p></div>):<p className="muted">No listings yet.</p>}</div>
<h2 className="adminSectionTitle">Provider verification<span>Service providers</span></h2><div className="adminList">{data.providers.length?data.providers.map((x:any)=><div key={x.id}><b>{x.business_name}</b><span>{x.provider_type} · {x.verification_status}</span>{buttons("admin_service_provider",x.id,x.verification_status,"verified")}</div>):<p className="muted">No providers yet.</p>}</div>
<h2 className="adminSectionTitle">Service catalogue<span>Service offerings</span></h2><div className="adminList">{data.offerings.length?data.offerings.map((x:any)=><div key={x.id}><b>{x.title}</b><span>{x.category} · {x.status}</span>{buttons("admin_service_offering",x.id,x.status)}</div>):<p className="muted">No services yet.</p>}</div>
<h2 className="adminSectionTitle">Commercial pipeline<span>CNG for Business enquiries</span></h2><div className="adminList">{data.business.length?data.business.map((x:any)=><div key={x.id}><b>{x.company_name} · {x.enquiry_type}</b><span>{x.contact_name} · {x.phone||x.email||"No contact"}{x.estimated_daily_scm?" · "+x.estimated_daily_scm+" SCM/day":""}</span><p>{x.message}</p><label>Pipeline status <select value={x.status} onChange={e=>businessStatus(x.id,e.target.value)}><option value="new">New</option><option value="contacted">Contacted</option><option value="qualified">Qualified</option><option value="closed">Closed</option></select></label></div>):<p className="muted">No business enquiries yet.</p>}</div>
<h2 className="adminSectionTitle">Network onboarding<span>Station registrations</span></h2><div className="adminList">{data.registrations.length?data.registrations.map((x:any)=><div key={x.id}><b>{x.name}</b><span>{x.operator_name||"Operator not stated"} · {x.address}, {x.city||""} {x.state||""}</span><p><button className="primary" onClick={()=>registration(x.id,true)}>Approve station</button> <button className="secondary" onClick={()=>registration(x.id,false)}>Reject</button></p></div>):<p className="muted">No pending station registrations.</p>}</div><h2 className="adminSectionTitle">Operator claims<span>Station claims</span></h2><div className="adminList">{data.claims.length?data.claims.map((x:any)=><div key={x.id}><b>{x.business_name||"Unnamed operator"}</b><span>{x.stations?.name||"Station"} · {x.status}</span>{x.status==="pending"&&<p><button className="primary" onClick={()=>claim(x.id,true)}>Approve</button> <button className="secondary" onClick={()=>claim(x.id,false)}>Reject</button></p>}</div>):<p className="muted">No claims yet.</p>}</div>
<h2 className="adminSectionTitle">Network intelligence<span>Community reports</span></h2><div className="adminList">{data.reports.length?data.reports.map((x:any)=><div key={x.id}><b>{x.stations?.name||"Station"} · {x.status||"unknown"}</b><span>{x.is_moderated?"Reviewed":"Awaiting review"}</span>{!x.is_moderated&&<p><button className="primary" onClick={()=>moderate(x.id,true)}>Apply to station</button> <button className="secondary" onClick={()=>moderate(x.id,false)}>Review only</button></p>}</div>):<p className="muted">No reports yet.</p>}</div></section></main>}