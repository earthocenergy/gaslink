"use client";
import {FormEvent,useState} from "react";import Link from "next/link";import BackButton from "@/components/BackButton";
export default function Trip(){const [from,setFrom]=useState(""),[to,setTo]=useState(""),[show,setShow]=useState(false);function go(e:FormEvent){e.preventDefault();setShow(true)}return <main><header><Link href="/" className="brand"><span className="mark">G</span><div><b>GasLink</b><small>by Earthoc Gas</small></div></Link><BackButton fallback="/dashboard"/></header><section className="panel"><h1>Plan a trip</h1><form onSubmit={go}><label>Starting point<input required value={from} onChange={e=>setFrom(e.target.value)}/></label><label>Destination<input required value={to} onChange={e=>setTo(e.target.value)}/></label><button className="primary">Plan journey</button></form>{show&&<p>{from} → {to}</p>}</section></main>}