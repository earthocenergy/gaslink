"use client";import {FormEvent,useState} from "react";import Link from "next/link";import BackButton from "@/components/BackButton";import {Navigation} from "lucide-react";
export default function Trip(){const [from,setFrom]=useState(""),[to,setTo]=useState(""),[range,setRange]=useState(250),[show,setShow]=useState(false);function go(e:FormEvent){e.preventDefault();setShow(true)}return <main><header><Link href="/" className="brand"><span className="mark">G</span><div><b>GasLink</b><small>by Earthoc Gas</small></div></Link><BackButton fallback="/dashboard"/></header><section className="panel toolPanel"><div className="eyebrow"><Navigation size={15}/>Trip planner</div><h1 className="toolTitle">Plan your CNG journey</h1><p className="muted">Enter your journey and vehicle CNG range. Phase 0 identifies GasLink refuelling options; full route intelligence comes next.</p><form onSubmit={go}><label>Starting point<input required value={from} onChange={e=>setFrom(e.target.value)} placeholder="e.g. Port Harcourt"/></label><label>Destination<input required value={to} onChange={e=>setTo(e.target.value)} placeholder="e.g. Aba"/></label><label>Estimated CNG range (km)<input required type="number" min="1" value={range} onChange={e=>setRange(+e.target.value)}/></label><button className="primary">Find refuelling options</button></form>{show&&<div className="authMsg"><b>{from} → {to}</b><br/>Vehicle range: {range} km. <Link href="/stations"><u>View GasLink stations</u></Link> that may support this journey.</div>}</section></main>}