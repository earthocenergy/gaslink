"use client";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import Link from "next/link";import BackButton from "@/components/BackButton";import {createClient} from "@/lib/supabase/client";export default function ServiceProvider(){const {id}=useParams<{id:string}>(),[p,setP]=useState<any>(null),[services,setServices]=useState<any[]>([]),[msg,setMsg]=useState("");useEffect(()=>{const db=createClient();db.from("service_providers").select("*").eq("id",id).single().then(({data})=>setP(data));db.from("service_offerings").select("*").eq("provider_id",id).eq("status","approved").then(({data})=>setServices(data||[]))},[id]);async function enquire(offering:string|null){const db=createClient(),{data:{user}}=await db.auth.getUser();if(!user){location.href="/auth";return}const message=prompt("Describe the CNG service you need:");if(message===null)return;const phone=prompt("Your contact phone number:");const {error}=await db.from("service_enquiries").insert({provider_id:id,offering_id:offering,customer_id:user.id,message,phone});setMsg(error?error.message:"Service enquiry sent.")}if(!p)return <main className="panel">Loading provider…</main>;return <main><header><Link href="/services" className="brand"><span className="mark">G</span><div><b>GasLink</b><small>CNG Services</small></div></Link><BackButton fallback="/services"/></header><section className="productDetail"><div className="productHero">CNG SERVICE</div><div><span className="demo">VERIFIED PROVIDER</span><h1>{p.business_name}</h1><h3>{p.provider_type}</h3><p>{p.address}, {p.city}, {p.state}</p><p>{p.description}</p><h2>Available services</h2>{services.map(x=><div className="trustBox" key={x.id}><b>{x.title}</b><p>{x.description}</p><button className="secondary" onClick={()=>enquire(x.id)}>Enquire</button></div>)}<button className="primary" onClick={()=>enquire(null)}>Contact provider</button>{msg&&<div className="authMsg">{msg}</div>}</div></section></main>}