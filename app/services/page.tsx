"use client";import {useEffect,useState} from "react";import Link from "next/link";import BackButton from "@/components/BackButton";import {createClient} from "@/lib/supabase/client";type P={id:string;business_name:string;provider_type:string;city:string|null;state:string|null;description:string|null};const types=["Conversion Centre","CNG Technician / Installer","Maintenance & Repair","Inspection & Testing","Station Engineering / EPC","Fleet Conversion Support","Emergency / Breakdown Support"];export default function Services(){const [items,setItems]=useState<P[]>([]),[type,setType]=useState("All");useEffect(()=>{createClient().from("service_providers").select("id,business_name,provider_type,city,state,description").eq("verification_status","verified").then(({data})=>setItems((data||[]) as P[]))},[]);const shown=items.filter(x=>type==="All"||x.provider_type===type);return <main><header><Link href="/" className="brand"><span className="mark">G</span><div><b>GasLink</b><small>CNG Services</small></div></Link><BackButton/></header><section className="marketHero"><span className="eyebrow">CNG Services</span><h1>Find trusted CNG support.</h1><p>Discover conversion centres, technicians, maintenance, inspection and engineering providers across the GasLink network.</p><div className="actionRow"><Link className="primary" href="/services/join">List your service</Link><Link className="secondary lightAction" href="/services/provider">Provider dashboard</Link></div></section><section className="marketWrap"><div className="categoryStrip"><button onClick={()=>setType("All")}>All services</button>{types.map(t=><button key={t} onClick={()=>setType(t)}>{t}</button>)}</div><div className="marketGrid">{shown.map(x=><Link href={"/services/"+x.id} className="productCard" key={x.id}><div className="productImage">SERVICE</div><span className="demo">VERIFIED PROVIDER</span><h3>{x.business_name}</h3><b>{x.provider_type}</b><p>{[x.city,x.state].filter(Boolean).join(", ")}</p><small>{x.description||"View provider and available CNG services"}</small></Link>)}{!shown.length&&<div className="emptyBox"><h2>Provider network is opening</h2><p className="muted">Approved CNG service providers will appear here.</p></div>}</div></section></main>}