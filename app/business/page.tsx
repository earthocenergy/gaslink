"use client";import {FormEvent,useState} from "react";import Link from "next/link";import BackButton from "@/components/BackButton";import {createClient} from "@/lib/supabase/client";const types=["Fleet CNG Supply","Bulk / Industrial CNG","Fleet Conversion","CNG Equipment Procurement","Station Development / EPC","Daughter Station / Supply Partnership","Other Commercial Enquiry"];export default function Business(){const [msg,setMsg]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const db=createClient(),{data:{user}}=await db.auth.getUser();if(!user){location.href="/auth";return}const f=new FormData(e.currentTarget);const {error}=await db.from("business_enquiries").insert({user_id:user.id,company_name:String(f.get("company")),contact_name:String(f.get("contact")),email:String(f.get("email")),phone:String(f.get("phone")),enquiry_type:String(f.get("type")),fleet_size:f.get("fleet")?Number(f.get("fleet")):null,estimated_daily_scm:f.get("scm")?Number(f.get("scm")):null,operating_locations:String(f.get("locations")),message:String(f.get("message"))});setMsg(error?error.message:"Commercial enquiry received. Earthoc Gas can now review it from the GasLink pipeline.");setBusy(false)}return <main><header><Link href="/" className="brand"><span className="mark">G</span><div><b>GasLink</b><small>For Business</small></div></Link><BackButton/></header><section className="marketHero"><span className="eyebrow">CNG FOR BUSINESS</span><h1>Power your fleet and operations with CNG.</h1><p>Connect with GasLink for fleet supply, bulk CNG, conversions, equipment procurement and station development opportunities.</p></section><section className="businessGrid"><div><h2>Commercial CNG, connected.</h2><p>GasLink turns commercial demand into actionable CNG opportunities for fleet operators, industries, station developers and supply partners.</p><div className="trustBox"><b>Fleet & bulk supply</b><p>Tell us your routes, fleet size and expected gas demand.</p></div><div className="trustBox"><b>Infrastructure & equipment</b><p>Source equipment or discuss mother/daughter station development.</p></div></div><form className="stationForm businessForm" onSubmit={submit}><label>Company name<input name="company" required/></label><label>Contact person<input name="contact" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" required/></label><label>Requirement<select name="type">{types.map(x=><option key={x}>{x}</option>)}</select></label><label>Fleet size (if applicable)<input name="fleet" type="number"/></label><label>Estimated daily CNG demand (SCM)<input name="scm" type="number"/></label><label>Operating locations / routes<input name="locations"/></label><label className="wide">Tell us what you need<textarea name="message" required/></label><button className="primary" disabled={busy}>{busy?"Sending…":"Submit business enquiry"}</button>{msg&&<div className="authMsg wide">{msg}</div>}</form></section></main>}